 ##Date:11-05-2026

name: Backend Deploy

on:
  push:
    branches:
      - cicd
    paths:
      - 'backend/**'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}

          script: |
            export PATH=$PATH:/home/ubuntu/n/bin

            cd /opt/apps/villagkart/cicd-test

            git checkout cicd

            git pull origin cicd

            cd backend

            npm install

            npx prisma generate

            npx prisma db push

            pm2 restart vsms-backend
