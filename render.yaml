services:
  - type: web
    name: villagkart-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install && npx prisma generate && npx prisma db push --accept-data-loss
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromConfigVar: DATABASE_URL
      - key: JWT_SECRET
        generateValue: true
      - key: SUPABASE_URL
        fromConfigVar: SUPABASE_URL
      - key: SUPABASE_KEY
        fromConfigVar: SUPABASE_KEY
      - key: PORT
        value: 5000
